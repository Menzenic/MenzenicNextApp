import { SexualHealthSection } from "@/components/client";
import { ChatBotQuestions } from "@/utils/constants";

export default function HealthAssesment() {
	return (
		<div className="h-full w-full bg-red-400 mt-40">
			{/* <SexualHealthSection
				optionType="question"
				options={item.options}
				title={item.title}
			/> */}
		</div>
	);
}
